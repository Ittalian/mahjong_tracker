# 検索・絞り込み機能の実装タスク

- [x] `lib/widgets/category_view.dart` の状態に `_activeFilters` (プロパティ名 -> 入力文字列) を追加
- [x] 画面上部のコンテナ右上に検索（フィルター）ボタンを追加
- [x] 検索ボタン押下時にボトムシート（フィルターUI）を表示する処理を実装
- [x] ボトムシート内に、カテゴリに応じた各プロパティのテキストフィールド（フリーワード検索用）と、「メモ」のテキストフィールドを実装
- [x] 各テキストフィールドで入力されたスペース区切りの文字列を、OR条件（またはAND条件）でフィルタリングするロジックを実装
- [x] 絞り込み条件（`_activeFilters`）が設定されている場合、リスト表示および合計金額の計算をフィルタリングしたデータで行うように修正
- [x] フィルタリング中であることがわかるよう、検索ボタンの色を変更するなどの視覚的フィードバックを追加
- [x] Dartの静的解析（`dart analyze`）を実行してエラーがないことを確認
